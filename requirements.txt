# Runtime dependencies only — these are the packages installed into the
# production container image. Test and lint tooling lives in requirements-dev.txt
# so it never ships to production.
fastapi==0.136.3
uvicorn==0.32.1
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==6.1.0
pydantic==2.10.3
